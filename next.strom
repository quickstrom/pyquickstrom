import quickstrom;

action disable! = click!(`button`);

let disabled = not (`[name=foo]`.enabled || `[name=bar]`.enabled);

let enabled = `[name=foo]`.enabled && `[name=bar]`.enabled;

let prop = always (enabled || disabled);

check prop with disable! when loaded?;