import quickstrom;

action disable! = click!(`button`);

let active = `[name=foo]`.enabled || `[name=bar]`.enabled;

let prop = active && next (not active) && next (next active);

check prop with disable! when loaded?;